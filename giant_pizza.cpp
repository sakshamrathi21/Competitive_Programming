#include<bits/stdc++.h>
using namespace std;
#define int long long

signed main () {
    ios::sync_with_stdio(0);
    cin.tie(0);
    int n, m;
    cin >> n >> m;
    vector<int> num_wishes(n+1, 0);
    vector<int> behaviour(m+1, 0);
    for (int i = 1 ; i <= n ; i ++ ){
        int a, b;
        char c, d;
        cin >> c >> a >> d >> b;
        // if (c == '+')
    }
}
